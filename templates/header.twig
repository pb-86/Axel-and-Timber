<!DOCTYPE html>
<html {{ site.language_attributes }}>

<head>
	<meta charset="{{ site.charset }}">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	{{ function('wp_head') }}
</head>

<body class="{{ body_class }}">
	{{ fn('wp_body_open') }}
	{% include "template-parts/header/skip-links.twig" %}	

	<div class="axel-wrapper">
		<header class="axel-header">

			{% include "template-parts/header/site-name.twig" %}
			{% include "template-parts/header/search-form.twig" %}
			{% include "template-parts/menus/header-menu.twig" %}

		</header>
	</div>
